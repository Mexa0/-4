// 1.	Создать массив из чисел и найти их сумму
// Напишите программу, которая запрашивает у пользователя 5 чисел, сохраняет их в массив, а затем вычисляет и выводит их сумму.

// let numbers = [];

// for (let i = 0; i < 5; i++) {
//   let num = parseFloat(prompt(`Введите число ${i + 1}:`));
//   numbers.push(num);
// }

// let sum = numbers.reduce((acc, num) => acc + num, 0);

// console.log(`Сумма введенных чисел: ${sum}`);
// alert(`Сумма введенных чисел: ${sum}`);

// // --------------------------------------------------------------------------------------------- //

// 2.	Найти максимальный и минимальный элементы массива
// Создайте массив из 10 случайных чисел (или введите вручную). Напишите программу, которая определяет и выводит на экран максимальное и минимальное число из массива.

// let numbers = Array.from(
//   { length: 10 },
//   () => Math.floor(Math.random() * 100) + 1
// );

// let maxNum = Math.max(...numbers);
// let minNum = Math.min(...numbers);

// console.log(`Массив: ${numbers}`);
// console.log(`Максимальное число: ${maxNum}`);
// console.log(`Минимальное число: ${minNum}`);

// --------------------------------------------------------------------------------------------- //

// 3.	Перевернуть массив
// Пусть есть массив из 5 элементов. Переверните его порядок, используя цикл. Например:
// ●	Ввод: [1, 2, 3, 4, 5]
// ●	Вывод: [5, 4, 3, 2, 1]

// let numbers = [1, 2, 3, 4, 5];
// let reversedArray = [];

// for (let i = numbers.length - 1; i >= 0; i--) {
//   reversedArray.push(numbers[i]);
// }

// console.log(`Исходный массив: ${numbers}`);
// console.log(`Перевернутый массив: ${reversedArray}`);

// --------------------------------------------------------------------------------------------- //

// 4.	Подсчитать количество четных и нечетных чисел в массиве
// Создайте массив из 10 чисел. Напишите программу, которая подсчитывает, сколько в массиве четных и нечетных чисел, и выводит результат на экран.

// let numbers = Array.from(
//   { length: 10 },
//   () => Math.floor(Math.random() * 100) + 1
// );

// let evenCount = 0;
// let oddCount = 0;

// numbers.forEach((num) => {
//   if (num % 2 === 0) {
//     evenCount++;
//   } else {
//     oddCount++;
//   }
// });

// console.log(`Массив: ${numbers}`);
// console.log(`Четных чисел: ${evenCount}`);
// console.log(`Нечетных чисел: ${oddCount}`);

// --------------------------------------------------------------------------------------------- //

// 5.	Проверить наличие элемента в массиве
// Запросите у пользователя 5 строк (тексты) и сохраните их в массив. Затем попросите пользователя ввести ещё одну строку и проверьте, есть ли эта строка в массиве. Выведите сообщение о наличии или отсутствии строки.

// ❌ //
// --------------------------------------------------------------------------------------------- //

// 6.	Создать массив с числами и удалить отрицательные
// Заполните массив из 10 чисел (вводом или случайными числами). Напишите программу, которая удаляет из массива все отрицательные числа и выводит оставшиеся элементы.

// let numbers = Array.from(
//   { length: 10 },
//   () => Math.floor(Math.random() * 101) - 50
// );

// let filteredNumbers = numbers.filter((num) => num >= 0);

// console.log(`Исходный массив: ${numbers}`);
// console.log(`Без отрицательных чисел: ${filteredNumbers}`);

// --------------------------------------------------------------------------------------------- //

// 7.	Вывести массив с уникальными значениями
// Создать массив с числами и вывести только уникальные значения (то есть без повторений).

// ❌ //
